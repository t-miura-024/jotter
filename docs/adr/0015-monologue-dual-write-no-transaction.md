---
status: accepted
---

# Monologueの2出力は非トランザクション・片失敗明示＋失敗側のみリトライにする

noteとGoogleの2出力は常時両方へ行い（選択不可）、分散トランザクションは組まない。片方失敗時はSSE errorでどちらが失敗したかを明示し、失敗側のみ再送できる。入力は保持する。

GitHubとGoogleにまたがるロールバックは困難であり、stateless方針とも衝突する。両再送による重複作成より、失敗側再送の方が安全である。一覧のnote/GCバッジで片方のみ状態を可視化し、手動補修を可能にする。
